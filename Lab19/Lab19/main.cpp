#include "heap.h"

int main()
{
// Once your heap functions are written you can use the following code to test it, or you can write your own.

/*	int size = 0;
	Heap h(MAXSIZE);
	int p;
	string s;

	do
	{
		cout << "Enter number of tasks (no more than " << MAXSIZE << "): ";
		cin >> size;
	} while (size < 1 && size > MAXSIZE);
 
	for (int i = 0; i < size; i++)
	{
		cout << "Enter priority level of task " << i << ": ";
		cin >> p;
		cout << "Enter description of task " << i << ": ";
		cin.ignore();
		getline(cin, s);
		Task temp(s,p);
		h.insert(temp);
	}

	h.print();

	Task t;
	for (int i = 0; i < size; i++)
	{
		t = h.pop();
		cout << "Popping: " << t.getPriority() << " " << t.getTask() << endl;
	}
*/
	system("PAUSE");
}